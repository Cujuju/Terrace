{
  "name": "terrace-monorepo",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "typecheck": "pnpm -r --if-present run typecheck",
    "test": "pnpm -r --if-present run test",
    "lint": "eslint .",
    "lint:seed": "node tools/eslint-plugin-terrace/bin/seed-budget.ts"
  },
  "devDependencies": {
    "eslint": "^10.10.0",
    "typescript": "6.0.3",
    "typescript-eslint": "^8.69.0"
  }
}
