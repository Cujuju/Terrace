node_modules/
dist/
data/
.env
*.tsbuildinfo
.claude/worktrees/

# Python bytecode from run_server.py
__pycache__/

# Throwaway visual-verification rigs (screenshots, capture scripts, vite logs).
.volcano-shots/
.mudslide-shots/
.weather-verify/
.gpu-perf/

# Third-party model sources, the .glb the import tool writes from them, and its
# render checks. Sources are downloaded, never committed (see LICENSES.md
# inside it); an asset that ships gets copied into its plugin's assets/ dir.
.model-import/
